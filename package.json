
{
  "name": "testcafe-demo",
  "version": "1.0.0",
  "description": "TestCafe demo project with JUnit reporting",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "npx testcafe chromium:headless tests/flaky-test.js --reporter junit:reports/results.xml",
    "test-login": "npx testcafe chromium:headless tests/flaky-test.js -t LoginTest --reporter junit:reports/LoginTest.xml",
    "test-signup": "npx testcafe chromium:headless tests/flaky-test.js -t SignupTest --reporter junit:reports/SignupTest.xml",
    "test-all": "npx testcafe chromium:headless tests/flaky-test.js --reporter junit:reports/results.xml"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "testcafe": "^3.7.2",
    "testcafe-reporter-junit": "^3.0.2"
  }
}
